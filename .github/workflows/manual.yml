name: Cypress API Tests

on: [push]

jobs:
  Cypress-Test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: npm install

      - name: Run Cypress Backend Tests
        uses: cypress-io/github-action@v6
